import type { Route } from '@/env';

export const routes: Route[] = [
  {
    path: '/',
    label: 'Home',
  },
  {
    path: '/projects',
    label: 'Projects',
  },
  {
    path: '/about',
    label: 'About',
  }
];

export default routes;
